#include <iostream>
using namespace std;

int main()
{
    int x = 5;
    x == 3;     // Equal to
    x != 3;     // Not equal
    x > 3;      // Greater than
    x < 3;      // Less than
    x >= 3;     // Greater than or equal to
    x <= 3;     // Less than or equal to
    return 0;
}
