#include <iostream
using namespace std;

int main()
{
    bool condition = true;
    if (condition) {
        // Execute this code if condition is true
    }

    int x = 20;
    int y = 18;
    if (x > y) {
        cout << "The number " << x << " is higher than " << y << endl;
    }

    bool isGreater = x > y;
    if (isGreater)
    {
        cout << "The number " << z << " is greater than " << y;
    }

    return 0;
}
