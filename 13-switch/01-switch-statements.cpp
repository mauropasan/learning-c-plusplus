#include <iostream>
using namespace std;

int main()
{
    int expression = 1;

    switch(expression) {
      case 1:
        // Execute if expression is 1;
        break; // Specifies to get out of the switch
      case 2:
        // Execute if expression is 2;
        break;
      default:
        // Execute if no condition above was made
    }
}
